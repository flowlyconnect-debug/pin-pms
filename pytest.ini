[pytest]
testpaths = tests
addopts = -ra --tb=short --cov=app --cov-report=term --cov-report=xml --cov-fail-under=80
markers =
    no_db_isolation: skip session app fixture and per-test DB wipes (unit tests without Postgres)
filterwarnings =
    ignore::DeprecationWarning
