# pindora-pms-sdk

```python
from pindora_pms import PMSClient

client = PMSClient(api_key="pms_xxx", base_url="https://api.example.com")
reservations = client.reservations.list(page=1, per_page=20)
```
